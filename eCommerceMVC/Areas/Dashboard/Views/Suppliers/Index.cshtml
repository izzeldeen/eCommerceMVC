
@model eCommerceMVC.Areas.Dashboard.ViewModels.SuppliersListingViewModel
@{
}

<div class="row mt-2">
    <div class="col-lg-12 col-md-12">
        <h2>Suppliers</h2>
        <hr />
    </div>
    <div class="col-md-12">
        <form action="@Url.ListAction("Suppliers")">
            <div class="input-group mb-3">


                <a href="@Url.CreateAction("Suppliers")" role="button" class="form-control btn btn-success"><i class="fas fa-plus"></i> Create Supplier</a>
            </div>
        </form>
    </div>
    <div class="col-lg-12">
        @{Html.RenderPartial("_Listing", Model);}
    </div>
</div>
